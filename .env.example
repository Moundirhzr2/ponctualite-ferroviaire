# Copy to .env (never committed) to pull the hosted collector's data locally.
# Both values are on the Supabase dashboard: Project Settings > API.
# The publishable key is read-only here: row level security allows SELECT only.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_PUBLISHABLE_KEY=
